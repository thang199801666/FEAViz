# Third-party dependencies

Phase 0 has no mandatory third-party runtime dependencies.

Future dependencies must be optional where possible and isolated from FEAViz public ABI. Do not place platform SDKs, OpenGL loaders, compression libraries, or format libraries directly into Core without an architecture review.
